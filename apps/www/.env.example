# Public base URL of the site (used for metadata and absolute URLs).
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Set to 1 in a preview deploy to expose the /dev review pages in a production build.
NEXT_PUBLIC_ENABLE_DEV_PAGES="0"
